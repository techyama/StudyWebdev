・percentage
    親要素や対象要素自身からの相対値。
    例）width: 50%; 親要素の半分のwidth

・em
    親要素や対象要素自身からの相対倍率。1emは同値。2emは2倍。
    例）width: 0.5em; 親要素の0.5倍のwidth

・rem(root em)
    ルート要素のfont-sizeを基準とした相対倍率。扱いやすいことが多い。
    例）ul: 3rem;



